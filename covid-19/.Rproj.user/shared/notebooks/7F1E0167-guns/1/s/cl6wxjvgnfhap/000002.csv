"0","```r
# guns registered

gowners %>%
    left_join(states,by=c(\"state\"=\"State\")) %>% 
    group_by(region) %>%
    summarise(total_reg=sum(guns_reg)) %>%
    ggplot(aes(x=reorder(region,total_reg),y=total_reg)) +
        geom_bar(stat=\"identity\",width=0.6,alpha=0.8,fill=\"#78c1f9\") +
        scale_y_continuous(labels=scales::comma) +
        coord_flip() +
        theme_minimal() +
        ylab(\"\") + xlab(\"\") +
        ggtitle(\"registered guns by region\")

```"

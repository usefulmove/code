---
title: "core"
output: html_document
---

```{r setup, include=FALSE}
library(tidyverse)
library(DBI)
library(RMariaDB)
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
cores <- read_csv("~/repos/code/cores/one")
```

```{r}
db <- dbConnect(
        MariaDB(),
        dbname = "b28kw3czffvrcmddjxri",
        host = "b28kw3czffvrcmddjxri-mysql.services.clever-cloud.com",
        port = 3306,
        user = "uregdz06i7ou4d65",
        password = "q6sGFI1tu1fsAvIPVFQh"
      )


cores_add <-
  cores %>%
    filter(id >= 40e3) %>%
    filter(id < 80e3) %>%
    arrange(id)


dbWriteTable(
  db,
  "cores",
  cores_add,
  append = TRUE
)
beepr::beep(5)
```
